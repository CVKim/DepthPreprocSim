# PC3 모션 가이드 Shoulder R 프로파일 튐 검토 (2026-09-17 15:29 회차, 이상 표본 2000026091715281105 직전)

데이터 `H:\000. PJT\01. HankookTire\03_이미지\MotionGuide\0917\3\TireInput_{0,1,2}_2.mim`(10행×1940열 float32, x = −97 + col×0.1 mm),
코드 `E:\talos-vision\src\Algorithm\alg_body\src\MotionGuideAlg.cpp`(teaching_mode 2), 파라미터
`D:\AIV\MODEL\[1]TireInspect_PC3_DEPLOY\MOTION_GUIDE\AlgorithmParameters\...DLMODEL0003+MotionGuideAlg.json`
(shoulder_ratio_k 0.1, median 31, safe z −35/−35, search x −97..60, dx2/dz2 = −1/0, dx3/dz3 = −1/−1).

## 결론
1. 파이썬 재현(`guide_sim.py`)이 현장 결과와 일치한다: Profile dZ −14.94, Mach X +3.936 / Z −14.432, J·J_inv 4자리까지 동일.
2. "튄" 지점(x −10.9, z −20.06)은 실제 골이 아니라 **x≈0 의 수직 벽(골→비드 측 +5 mm 로 급상승) 인접 구간의 삼각측량 이상치**다.
   x −12..−1 구간에서 10행 중 1~2행만 −20~−33 mm 를 읽고 나머지는 −12~+4 mm(열 내 산포 5~27 mm, 평탄면은 ≤0.5 mm).
   `shoulder_ratio_k 0.1` × 10행 → k = 1 이므로 bottom 프로파일이 **열별 최소값 1점**이 되어 이 이상치를 그대로 채택했다(3회 스캔 모두 같은 구간에서 −20~−30 발생).
3. 그러나 **기계 이동량은 사실상 바뀌지 않았다.** 이상치를 걷어낸 어떤 대안(k 0.3/0.5, 중앙값, 평균)도 진짜 골(x −22, z −15.4)을 잡아 필요 이동 −16.6~−19.6 mm 가 되고, 알고리즘의 15 mm 소프트 리밋에 걸려 −15.00 → Mach (+3.952, −14.492). 현장값 (+3.936, −14.432) 과 차이 0.06 mm. 방향도 같다.
   즉 이 회차의 Shoulder R 이동은 "튐 때문에 잘못된 방향"은 아니다. 다만 리밋 때문에 골이 목표 −35 가 아닌 약 −30 에 놓였다(정상 표본도 같은 조건).
4. Mach X +3.9 mm 는 프로파일 dX 0 인데도 나온 값으로, J_inv 의 결합항(Z 이동 → 프로파일 X 변화 −0.26 배)에 의한 보상이다. J 는 센서가 기계축 대비 약 20°(R)/33°(L) 기울고 R 은 X 가 반전된 형상과 일치해 물리적으로 타당하다. 단, 1 mm 보정 스텝을 0.1 mm 분해능으로 측정해 J 원소당 ±10% 오차가 있고, 15 mm 이동 시 ±1.5 mm 수준 오차가 생긴다.
5. 정상처럼 보이는 표본(1639390184_…)과 R 검사 이미지를 비교하면 타이어 띠 행 범위 814~1808 vs 749~1758, z 범위 −53..19 vs −55..35 로 센서 위치는 비슷하다. 이상 표본의 특이점은 가이드가 아니라 **회전 중 축방향 흔들림 15.9 mm(상부)** 와 **L 센서 측정 범위 ±45 mm 포화**(L 검사 이미지 z 가 정확히 ±45.000 에서 끊김) 쪽에 있다.

## 가이드 알고리즘 개선 후보
| # | 항목 | 현재 | 제안 |
|---|---|---|---|
| 1 | bottom 프로파일 | k = ceil(10×0.1) = 1 → 열 최소값 | `shoulder_ratio_k` 0.3~0.5(하위 3~5점 평균) 또는 중앙값, 열 내 산포 > 3 mm 열은 NaN 처리 |
| 2 | 탐색 구간 | search x −97..60 → 벽(x≈0)과 지그(x 55~60) 포함 | 골 탐색은 벽 좌측(예 −70..−5)로 제한, 지그 제외 |
| 3 | Jacobian | 1 mm 스텝, 평균 프로파일(이상치 포함, 스캔1 MAE 0.78) | 스텝 2~3 mm 또는 중앙값 프로파일 사용, 골 구간만으로 정합 |
| 4 | 15 mm 리밋 | 필요 −19.6 을 −15 로 절단, 로그만 남김 | 2단 이동(재가이드) 또는 리밋 초과 시 경보 |

## 근거 파일
- `guide_sim.py` 재현(모드 1·2), `guide_sim.json` 수치, `spike.py`/`spike.json` 열별 분석·대안 비교, `plot_spike.py` → `shoulderR_guide_spike_analysis.png`
- 현장 시각화 사본 `RawProfile_{0,1,2}.png`, `ResultDrawImg_{0,1,2}.png`(0 비드, 1 숄더 L, 2 숄더 R)
